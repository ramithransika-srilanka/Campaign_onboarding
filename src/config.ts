// Where "Go to Campaign" sends the user once onboarding is complete.
export const CAMPAIGN_URL = import.meta.env.VITE_CAMPAIGN_URL ?? '#';

// How long the brand splash stays on screen before moving to sign-in.
export const SPLASH_DURATION_MS = 1800;

// Length of the splash logo's fade-out before the welcome screen appears.
export const SPLASH_EXIT_MS = 400;
